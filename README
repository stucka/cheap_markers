What you want is probably in the placemarkers.xls file.

This recreates some basic map markers using ImageMagick.

Base64 versions, which may be embedded directly into the HTML, are also included.

The pins are sorta kinda long the lines of the Google Fusion Tables
map markers with similar names.

This project made much more sense when a second allergy medication was
combined with Peroni.
